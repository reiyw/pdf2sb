name: Python package
on:
  push:
    tags:
      - "v*.*.*"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build and publish to PyPI
        uses: JRubics/poetry-publish@v1.15
        with:
          python_version: "3.10"
          pypi_token: ${{ secrets.PYPI_TOKEN }}
          extra_build_dependency_packages: "libjpeg-dev"
